package assignment.web.responses;

/**
 * The type of the client response
 * <p>
 * Move - The client is sending a game move
 * Ready - The client is setting its ready state
 */
public enum ClientResponseType
{
    MOVE, READY
}
